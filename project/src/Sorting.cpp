#include "ds/Sorting.h"

void sortItemsByValueDescending(Item* items, int count) {
    // TODO(Student): Implement a sorting algorithm.
    // Suggested: selection sort by item.getValue(), descending.
    (void)items;
    (void)count;
}

void sortScoresDescending(ScoreRecord* records, int count) {
    // TODO(Student): Implement a sorting algorithm.
    // Suggested: insertion sort by score, descending.
    (void)records;
    (void)count;
}
